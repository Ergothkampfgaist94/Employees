@if (country is null)
{
    <Employees.Frontend.Components.Pages.Shared.Loading />
}
else
{
    <MudDialog>
        <DialogContent>
            <Employees.Frontend.Components.Pages.FormwithName.FormWithName Label="País:" Model="country" OnValidSubmit="EditAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}